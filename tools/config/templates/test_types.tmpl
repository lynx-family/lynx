// Copyright 2025 The Lynx Authors. All rights reserved.
// Licensed under the Apache License Version 2.0 that can be found in the
// LICENSE file in the root directory of this source tree.

// THIS FILE IS AUTO-GENERATED. DO NOT EDIT MANUALLY.
// Generated by lynx/tools/config/gen_config.py from lynx_config.yml

import { describe, expectTypeOf, it } from 'vitest';
import { Config, CompilerOptions } from '../types/index';
{% if not export %}
import { CompilerOptionAirMode } from '../types/index';
{% endif %}

describe('Test Config Types', () => {
  it('should have correct Config type', () => {
    expectTypeOf<Config>().toBeObject();
    {% for config in configs %}
    {% if not export or (export and config.export)%}
    expectTypeOf<Config>().toHaveProperty('{{ config.name }}').toEqualTypeOf<{{ config.js_value_type }} | undefined>();
    {% endif %}
    {% endfor %}
  });
});

describe('Test Compile Options Types', () => {
  it('should have correct types', () => {
    expectTypeOf<CompilerOptions>().toBeObject();
    {% for option in options %}
    {% if not export or (export and option.export)%}
    expectTypeOf<CompilerOptions>().toHaveProperty('{{ option.name }}').toEqualTypeOf<{{ option.js_value_type }} | undefined>();
    {% endif %}
    {% endfor %}
  });
});
