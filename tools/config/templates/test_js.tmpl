// Copyright 2025 The Lynx Authors. All rights reserved.
// Licensed under the Apache License Version 2.0 that can be found in the
// LICENSE file in the root directory of this source tree.

// THIS FILE IS AUTO-GENERATED. DO NOT EDIT MANUALLY.
// Generated by lynx/tools/config/gen_config.py from lynx_config.yml

import { describe, it, expect } from 'vitest';
import { compilerOptionsKeys, configKeys } from '../index'

describe('type-config index', () => {
  it('should export correct keys and types', () => {
    expect(compilerOptionsKeys).toBeDefined();
    expect(configKeys).toBeDefined();
  });
  it('should have correct compilerOptionsKeys', () => {
    expect(compilerOptionsKeys.length).toBe({{ options | selectattr('export') | list | length if export else options | length }});
    expect(compilerOptionsKeys).toEqual([
      {% for option in options %}
      {% if not export or (export and option.export)%}
      '{{option.name}}',
      {% endif %}
      {% endfor %}
    ])
  })
  it('should have correct configKeys', () => {
    expect(configKeys.length).toBe({{ configs | selectattr('export') | list | length if export else configs | length }});
    expect(configKeys).toEqual([
      {% for config in configs %}
      {% if not export or (export and config.export)%}
      '{{config.name}}',
      {% endif %}
      {% endfor %}
    ]);
  });
});
