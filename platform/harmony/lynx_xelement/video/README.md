Lynx XElement Video
===================

Lynx XElement Video provides the open source `<video>` element on Harmony.

Note: ArkUI `Video` does not expose per-instance volume control. Harmony treats `volume=0` as muted, and non-zero volume plays at the system-managed volume level.
